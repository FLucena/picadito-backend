# H2 Database Configuration (Development)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (solo para desarrollo, deshabilitar en producción)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# Contraseñas de usuarios demo (solo para desarrollo)
# En producción, este perfil no se usa y DataInitializer está deshabilitado
# Para cambiar las contraseñas, usar variables de entorno:
# app.demo.admin.password=${ADMIN_DEMO_PASSWORD:AdminDemo2024!}
# app.demo.cliente.password=${CLIENTE_DEMO_PASSWORD:ClienteDemo2024!}
app.demo.admin.password=${ADMIN_DEMO_PASSWORD:AdminDemo2024!}
app.demo.cliente.password=${CLIENTE_DEMO_PASSWORD:ClienteDemo2024!}

